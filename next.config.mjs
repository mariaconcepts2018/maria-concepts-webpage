/** @type {import('next').NextConfig} */
const nextConfig = {
       images: {
        domains: ['images.unsplash.com', 'mariaconstructions.in', 'media.istockphoto.com'],
    },
};

export default nextConfig;
